# H2 Database
spring.h2.console.enabled=true

#configure h2 to store on disk
#spring.datasource.url=jdbc:h2:file:/home/don/secrets/myProjects/myDb

#configure h2 to store in memory. does not persist after application is closed
spring.datasource.url=jdbc:h2:mem:myDb

